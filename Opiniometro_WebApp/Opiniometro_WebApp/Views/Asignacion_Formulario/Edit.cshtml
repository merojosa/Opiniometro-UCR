@model Opiniometro_WebApp.Models.Tiene_Grupo_Formulario

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Edit</title>
</head>
<body>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        
        <div class="form-horizontal">
            <h4>Tiene_Grupo_Formulario</h4>
            <hr />
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            @Html.HiddenFor(model => model.SiglaCurso)
    
            @Html.HiddenFor(model => model.Numero)
    
            @Html.HiddenFor(model => model.Anno)
    
            @Html.HiddenFor(model => model.Ciclo)
    
            @Html.HiddenFor(model => model.Codigo)
    
            @Html.HiddenFor(model => model.FechaInicio)
    
            @Html.HiddenFor(model => model.FechaFinal)
    
            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Save" class="btn btn-default" />
                </div>
            </div>
        </div>
    }
    
    <div>
        @Html.ActionLink("Back to List", "Index")
    </div>
</body>
</html>
