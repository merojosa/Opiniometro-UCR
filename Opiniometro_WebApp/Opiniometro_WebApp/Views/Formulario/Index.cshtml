@model IEnumerable<Opiniometro_WebApp.Models.Formulario>

@{
    ViewBag.Title = "Formularios";
}

<h2>Formularios</h2>

<p>
    <input type="submit" value="Crear Nuevo Formulario" class="btn btn-secondary" id="botonNuevaSeccion" onclick="location.href = 'http://localhost:51103/Formulario/create'()" />
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Código Formulario")
        </th>

        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CodigoFormulario)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.CodigoFormulario }) |*@
            @*@Html.ActionLink("Details", "Details", new { id=item.CodigoFormulario }) |*@
            @*@Html.ActionLink("Delete", "Delete", new { id=item.CodigoFormulario })*@
        </td>
    </tr>
}

</table>
